name: CI

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: formating
        run: yarn
        
      - name: lint
        run: yarn format
